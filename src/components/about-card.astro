---
interface AboutCardProps {
  heading: string;
  content: string;
  icon: string;
}

const { heading, content, icon } = Astro.props;
---

<div class="aboutCard">
  <i class={icon}></i>
  <h3>{heading}</h3>
  <p>{content}</p>
</div>
